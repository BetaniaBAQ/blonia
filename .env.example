# Convex Deployment (set automatically by `convex dev`)
CONVEX_DEPLOYMENT=dev:your-team-project

# Client-side Convex URL (must have VITE_ prefix)
# Set this to your Convex deployment URL
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# NOTE: CONVEX_URL is set automatically by `convex dev` at runtime
# Do NOT manually add CONVEX_URL to this file to avoid CLI warnings

# WorkOS Authentication
# Get these from your WorkOS Dashboard: https://dashboard.workos.com
WORKOS_API_KEY=sk_test_...
WORKOS_CLIENT_ID=client_...

# WorkOS Redirect URI (where WorkOS redirects after authentication)
# For local development, use: http://localhost:3000/api/auth/callback
# For production, use: https://yourdomain.com/api/auth/callback
WORKOS_REDIRECT_URI=http://localhost:3000/api/auth/callback

# WorkOS Cookie Password (32+ characters, generate with: openssl rand -base64 32)
# This is used to encrypt session cookies
WORKOS_COOKIE_PASSWORD=your-32-character-secret-key-here

# Node Environment (optional, defaults to development)
NODE_ENV=development
